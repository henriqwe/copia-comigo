table:
  name: Telefones
  schema: contatos
event_triggers:
- definition:
    delete:
      columns: "*"
    enable_manual: true
    insert:
      columns: "*"
    update:
      columns:
      - created_at
      - Telefone
      - Tipos
      - Categorias
      - updated_at
      - NomeDoResponsavel
      - Identidades
      - deleted_at
      - Id
  name: Logs_contatos_Telefones
  retry_conf:
    interval_sec: 10
    num_retries: 0
    timeout_sec: 60
  webhook: "{{LOGS_EVENT_BASE_URL}}"
