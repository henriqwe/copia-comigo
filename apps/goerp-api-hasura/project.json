{
  "root": "apps/goerp-api-hasura",
  "sourceRoot": "apps/goerp-api-hasura",
  "projectType": "application",
  "targets": {
    "hasura": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": [
          {
            "command": "cd apps/goerp-api-hasura && hasura console --api-port 9602 --console-port 9702"
          }
        ]
      }
    },
    "hasura-update": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": [
          {
            "command": "cd apps/goerp-api-hasura && hasura metadata apply && hasura migrate apply --all-databases && hasura metadata reload"
          }
        ]
      }
    },
    "dcompose": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "commands": [
          {
            "command": "docker-compose -f apps/goerp-api-hasura/docker-compose.yaml up -d"
          }
        ]
      }
    },
    "echo": {
      "executor": "./tools/executors/echo:echo",
      "options": {
        "textToEcho": "Hello World"
      }
    }
  },
  "tags": []
}
